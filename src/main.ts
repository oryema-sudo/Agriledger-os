/**
 * AgriLedger – Application entry point (Phase 0 placeholder)
 *
 * Full NestJS bootstrap will be completed in Phase 1.
 */

console.log('AgriLedger – Phase 0 Architecture Baseline');
console.log('See /docs for product, architecture, database, RBAC, reconciliation and security specifications.');
console.log('Next: Phase 1 – Foundation (NestJS skeleton, Prisma, Auth, Organizations, RBAC).');

// Placeholder so that `node src/main.ts` or future tsc does not fail completely.
export {};
